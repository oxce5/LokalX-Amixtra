import React from "react";

const Slideshow: React.FC = () => (
  <div style={{
    width: 200,
    height: 300,
    background: "#fff",
    borderRadius: 12,
    boxShadow: "0 2px 8px rgba(0,0,0,0.1)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center"
  }}>
    <span style={{ color: "#aaa" }}>Slideshow Placeholder</span>
  </div>
);

export default Slideshow;
